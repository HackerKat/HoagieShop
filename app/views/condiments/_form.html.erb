<%= form_for(condiment) do |f| %>
  <% if condiment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(condiment.errors.count, "error") %> prohibited this condiment from being saved:</h2>

      <ul>
      <% condiment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :spiceindex %>
    <%= f.number_field :spiceindex %>
  </div>

  <div class="field">
    <%= f.label :amount %>
    <%= f.number_field :amount %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
